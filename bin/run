#!/usr/bin/env ruby

require_relative '../lib/renuo_blocks'

Thread.new { RenuoBlocks.start_mining(port: 9999, neighbors: [{ host: 'localhost', port: 9998 }]) }
Thread.new { RenuoBlocks.start_mining(port: 9998, neighbors: [{ host: 'localhost', port: 9999 }]) }

# current_block = RenuoBlocks.last_block
# until current_block.nil?
#   puts current_block.data
#   current_block = current_block.previous
# end
